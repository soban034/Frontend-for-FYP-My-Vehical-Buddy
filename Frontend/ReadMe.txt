Here in this we have two files, 



the Car Price prediction frontend takes the user input and sends the data to the flask server which integrates the frontend with Python Backend. The model then predicts the price and sends bHere in this we have two files, the Car Price prediction frontend takes the user input and sends the data to the flask server which integrates the frontend with Python Backend. The model then predicts the price and sends bHere in this we have two files, the Car Price prediction frontend takes the user input and sends the data to the flask server which integrates the frontend with Python Backend. The model then predicts the price and sends back the data to the user. User gets the predicted price and then post the vehicle in the store for other users to buy it.


In Feedback Help and support user send their feedback, reviews to the shopowner. There is a faq page which shows the answer to most commonly asked questions.
